# claude-in-docker
Allows you to easily run Claude code in a docker container in yolo mode with separate persistent profiles and access to multiple repositories. It can even start up its own docker services and dev servers to use to develop, troubleshoot, and test.
